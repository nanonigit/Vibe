# 再発防止

- アイコンだけのメニューでは標準開閉記号を明示的に隠すテストを行う。
- シャッフルのオン／オフと再生対象範囲を別の状態として表現する。
- 大規模ライブラリの選択はインデックスを使うDBクエリ内に留め、全件を配列化しない。
- キャッシュ記録はファイルの実在確認後にのみ再生可能として扱う。
- ストレージを使う項目の件数を表示するときは容量合計も扱う。
- 通常表示とコンパクト表示のフレームを別概念として保存し、復元時は表示可能画面内へ収める。

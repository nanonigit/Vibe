# 再発防止: ジャンル表記の標準化

- 取り込み、直接更新、一覧、件数、遷移フィルターで同じ標準化境界を使います。
- 外部データに新しい表記が現れたら、別名の回帰テストを追加します。
- 英語表示のために、未判定の非ラテン文字を削除しません。
- 大規模ライブラリでは先に異なるラベルだけへ縮約してからSwift側で正規化します。
- 曲テーブルへ列を追加した変更では、直接SQLを使う全テストfixtureも同時に更新・実行します。

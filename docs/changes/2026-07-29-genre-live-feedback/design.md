# Genre Live Feedback Design

- Diff the in-memory `facets` immediately after a successful save: increment existing counts or insert/sort new genres.
- Use a bilingual static characteristic catalog so the UI never implies audio-derived evidence.
- Following Lazyweb genre-browse examples, keep list descriptions to one line and place richer context in the detail header.
